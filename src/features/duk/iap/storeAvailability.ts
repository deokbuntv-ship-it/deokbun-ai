// 이 기기에서 **덕을 살 수 있는가** — 충전 입구를 열지 말지 정하는 단 하나의 스위치 (F-02 · 2026-09-21).
//
// 무엇이 문제였나 (2026-09-18 전수 조사)
//   충전으로 가는 입구 어디에도 플랫폼 확인이 없었다. 그래서
//     · 아이폰 앱: "구매" 가 살아 있는데 애플 결제는 이번 범위가 아니라 세 겹으로 실패한다
//       (요청 키 · 구글 상품 id · 서버가 애플 구매를 `NOT_CONFIGURED` 로 거절).
//     · 웹(아이폰 · 안드로이드 브라우저): 충전 화면이 "준비 중" 인데 링크는 전부 보여 막다른 길이었다.
//
// CTO 판정 (2026-09-18)
//   · 숨기는 것은 **충전 묶음(20 · 50 · 120덕)만.** 상담 · 궁합 · 리포트는 가입 덕 · 촛불 덕으로 계속 쓴다.
//   · **웹에서도 숨긴다.** 결제가 안 되는 것은 같다.
//   · **안내 문구 없이 숨긴다.** "준비 중" 은 애플 심사에 불리하다(가이드라인 2.1 임시 콘텐츠).
//
// ⚠ **애플 계정이 나오면 여는 조건** — 아래가 전부 끝난 뒤에만 `ios` 를 참으로 바꾼다:
//   ① Apple Developer 계정 · App Store Connect 앱 등록(`eas.json` 의 `submit.production` 이 지금 비어 있다)
//   ② 인앱 상품 3종 등록(id 는 `purchaseUiText.ts` 에 이미 정해 둠) · 서버 `product_catalog` 에 APPLE 행
//   ③ `verify-purchase` 의 애플 검증 구현(지금은 `NOT_CONFIGURED`) + App Store 서버 알림 V2 연결
//   ④ 충전 화면이 iOS 에서 **애플 상품 id** 로 요청(`duk-topup.tsx` 가 지금 구글 id 고정)
//   ⑤ 샌드박스 **실구매 E2E** 통과(구매 → 지급 → 마무리 → 환불 알림)
//   같은 목록이 `owner_inputs/다음묶음_작업목록_2026-09-17.md` §8-2 에도 있다.
import { Platform } from 'react-native';

/**
 * 덕을 살 수 있는 스토어가 이 기기에 있는가.
 *
 * 지금은 **안드로이드 앱만** 참이다. iOS 는 애플 결제가 준비되면(위 조건) 열고, 웹은 스토어 자체가 없다.
 */
export function storePurchaseAvailable(): boolean {
  return Platform.OS === 'android';
}
